<h1><%= @page.title %></h1>

<% if user_signed_in? && current_user.is_admin %>
  <div>
  <%= link_to t('actions.edit'), edit_page_path(@page) %> |
  <%= link_to t('actions.all_pages'), pages_path %>
  </div>
<% end %>

<%= raw @page.content %>