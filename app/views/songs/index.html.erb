<h1><%= t 'songs.archive' %></h1>

<%= link_to t('songs.new'), new_song_path %>

<ul>
<!-- asdasdsadsa -->
<% @songs.each do |song| %>
  <li>
    <%= link_to song.name, song %>
  </li>
<% end %>
</ul>